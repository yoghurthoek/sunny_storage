import numpy as np

class grid():
    """attributes"""

    def __init__(self, hood):
        self.grid = create_grid()
        self.houses =
        self.batteries =
        self.cables =

    def create_grid(self):
        x = np.linspace(0, 3, 4)
        y = np.linspace(0, 2, 3)
        xx, yy = np.meshgrid(x, y)


    def load_hood():
